version: "3"

services:
  backend:
    image: 010709min/backend
    container_name: backend
    hostname: backend
    env_file:
      - .env
    expose:
      - "8080"


  nginx:
    image: 010709min/nginx
    depends_on:
      - backend
    restart: always
    ports:
      - "80:80"